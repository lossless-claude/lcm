---
"@lossless-claude/lcm": minor
---

Add native Codex lifecycle hooks for automatic restoration, prompt recall, incremental capture, and continuity across compaction. Preserve unrelated hooks and report configuration separately from host trust and activation.

Include active and archived Codex sessions in the default import replay selection, reuse incremental ingestion across hooks and imports, and keep internal Codex summarization out of captured history.
