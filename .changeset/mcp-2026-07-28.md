---
"@lossless-claude/lcm": minor
---

Migrate the stdio MCP server to protocol revision 2026-07-28 with TypeScript SDK v2. Clients must support this revision; legacy initialize-based connections are rejected. The seven tools and both entrypoints remain available, and the doctor MCP probe uses the new protocol.
