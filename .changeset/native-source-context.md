---
"@lossless-claude/lcm": patch
---

Return bounded source context around native episodic search matches, with exact character spans and source hashes. Preserve ranking and regex/grep behavior while making retrieved explanations easier to read.

Describe the actual promoted-memory layer in the native MCP search schema.
