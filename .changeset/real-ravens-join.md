---
"lossless-claude": patch
---

Document VS Code and Codex connector setup, add the CLI memory commands used by connector skills, and fix linked `lcm` installs so symlinked binaries and authenticated daemon commands work correctly.