---
"@lossless-claude/lcm": minor
---

Add an opt-in QMD SDK search backend with explicit lexical/embedding indexing, worker-isolated hybrid retrieval, source-revision validation and visible native fallback. Expose it through CLI and MCP while retaining native search as the default.
